# Frontend (Create React App)

This project uses `react-scripts`, not Vite.

```bash
cp .env.example .env
npm install
npm start
```

Open `http://localhost:3000`. Start the simulator with phone `919876543210` and message `hii` after seeding the backend.

